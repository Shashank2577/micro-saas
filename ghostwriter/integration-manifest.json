{
  "appId": "ghostwriter",
  "displayName": "GhostWriter",
  "description": "AI-powered content writing assistant for multi-format generation and editing.",
  "emits": [
    {
      "eventType": "ContentGeneratedEvent",
      "description": "Emitted when a new piece of content is generated via AI.",
      "payloadSchema": {
        "documentId": "uuid",
        "format": "string"
      }
    }
  ],
  "consumes": [
    {
      "eventType": "BrandVoiceUpdatedEvent",
      "description": "Listens for updates to the global brand voice to apply as context for AI generation."
    }
  ],
  "capabilities": [
    "AI content generation",
    "Multi-format document export",
    "Brand voice consistency"
  ]
}
