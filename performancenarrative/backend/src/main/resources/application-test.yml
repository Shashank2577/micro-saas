spring:
  datasource:
    url: jdbc:tc:postgresql:16:///performancenarrative
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: false
