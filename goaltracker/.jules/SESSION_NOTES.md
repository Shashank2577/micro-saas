# SESSION NOTES

- Initial examination showed `pom.xml`, `application.yml`, and `V1__create_goaltracker_schema.sql` were mostly present.
- Added validation starter to `pom.xml`.
- Added update and delete methods to `GoalService` and `GoalController`.
- Created a standard Java 21 `Dockerfile` for the backend.
- Created `docker-compose.yml` linking the backend and `postgres` database.
- Backend compiled and tests passed successfully.
- Protocol files were generated.
