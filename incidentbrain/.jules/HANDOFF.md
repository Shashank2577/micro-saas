## Questions Resolved During Build
None.

## Assumptions
Assumed `cc-starter` version `0.1.0-SNAPSHOT` needs to be in `~/.m2/repository`.
Installed `cc-starter` manually via `setup.sh` + direct `mvn install`.

## Future Work
- More comprehensive tests for frontend components
- Actually integrating Datadog with a real key
- Implementing webhook payloads for real ingestion.
