server:
  port: 8093
spring:
  application:
    name: deploysignal
  datasource:
    url: jdbc:postgresql://localhost:5432/deploysignal
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
