## Questions Resolved
- cc-starter dependency could not be resolved because it wasn't published locally, so I created a stub `cc-starter` project and `mvn install`ed it locally.

## Future Work
- The frontend Next.js application needs to be developed
- Tests need to be written
