# HealthcareDocAI Session Notes

- Found scaffold of backend using Spring Boot + Postgres + `cc-starter` module.
- `cc-starter` installation initially failed because it was not installed locally. Had to run `mvn -f cross-cutting/pom.xml install -DskipTests -q`.
- Frontend scaffold available in `healthcaredocai/frontend` using Next.js. Tests run successfully.
- Will create proper plan files and update the repo following the autonomous protocol.
