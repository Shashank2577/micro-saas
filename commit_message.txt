feat(peopleanalytics): full autonomous implementation

Closes: N/A
Changes:
- Expanded detailed spec into .jules/DETAILED_SPEC.md.
- Created all backend JPA entities with tenant_id logic.
- Implemented backend services and REST controllers mapping to all domain endpoints.
- Updated Flyway migration and H2 dialect mapping to support local automated testing.
- Created Next.js React pages for all domain areas with loading and rendering logic.
- Added and configured vitest tests for all new pages to ensure testability.
- Updated root folder with standalone Dockerfile and README setup guide.

Testing:
- Backend: Tests pass locally via `mvn clean verify` with 100% build success.
- Frontend: `vitest` suite passes completely and Next.js static build operates with zero errors.

Spec Reference: See peopleanalytics/.jules/DETAILED_SPEC.md
Handoff Notes: See peopleanalytics/.jules/HANDOFF.md
