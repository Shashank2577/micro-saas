spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///testdb
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: brandvoice
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

cc:
  tenancy:
    mode: header
    header-name: X-Tenant-ID
